; A positive ray offers a greeting; a negative ray requests it.
; `show` displays the result of their fusion.

(def greeter [(+hello X) (hello X)])
(show (exec #greeter [(-hello world)]))  ; => (hello world)
