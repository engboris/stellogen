(library
 (name lsc)
 (libraries base common))

(env
 (dev
  (flags
   (:standard -warn-error -A))))

(menhir
 (modules ../common/parser parser)
 (merge_into parser)
 (flags --dump --explain))

(ocamllex lexer)
