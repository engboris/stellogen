(library
 (name stellogen)
 (libraries base common lsc))

(env
 (dev
  (flags
   (:standard -warn-error -A))))

(menhir
 (modules ../common/parser ../lsc/parser parser)
 (merge_into parser)
 (flags --dump --explain))

(ocamllex lexer)
